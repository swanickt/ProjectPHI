# Manual Review: 10000-Note Eval Chunk 02

Date regenerated: 2026-06-09

## Scope And Method

Reran the current ProjectPHI CLI against the local 2000-row chunk 02 input
file after the latest bounded pathology/classification, abbreviation, and
vendor/device semantic-preservation guards.

Regenerated only this run folder's `deidentified_output.csv`,
`audit_output.csv`, `audit_summary.csv`, `issue_candidates.csv`,
`row_review_summary.csv`, `failure_diagnostics.csv`, and this review.

The review compared parsed input rows, output rows, audit rows, warning rows,
source-group counts, changed/unchanged text, residual contact-like output
patterns, and pyDeid fallback replacements. It is a smoke review without gold
labels.

## Pipeline Summary

```text
rows_read=2000
rows_written=2000
rows_failed=0
spans_written=3111
warnings=2
```

Input/output row IDs and source-group counts matched for all 2000 rows. No
output row had empty `note_text`.

Among written rows, 1386 were text-identical to input and 614 changed through
de-identification, date shifting, or semantic-preservation reconstruction.

Audit span labels:

- `DATE`: 1481
- `NAME`: 1405
- `TIME`: 129
- `LOCATION`: 53
- `HOSPITAL`: 32
- `CONTACT`: 8
- `ID`: 3
- row warning records: 2

Replacement sources:

- `project_stable_date_shift`: 917
- `pyDeid`: 893
- `preserved`: 693
- `project_protected_clinical_term`: 342
- `project_clinical_code_veto`: 132
- `project_ordinary_token_veto`: 61
- `project_ordinary_clinical_prose_veto`: 39
- `project_clinical_abbreviation_veto`: 24
- `project_title_context_action_word_veto`: 7
- row warning records: 2
- `project_decimal_code_veto`: 2
- `project_obstetric_history_veto`: 1

Row review statuses:

- 1045 unchanged with no candidate flags
- 518 mitigated-only
- 437 review candidates present
- 0 failed

The row-review candidate count is generated by the current conservative
heuristic scan and is not directly comparable to older chunk-2 reports that
used narrower candidate grouping.

## Changes Since Prior Chunk-2 Review

The latest preservation work improved the direct audit counts:

- pyDeid fallback spans dropped from 1034 in the prior report to 893.
- Protected clinical-term mitigations increased from 267 to 342.
- Clinical-code/vendor/contextual mitigations increased from 91 to 132.
- Ordinary clinical prose mitigations increased from 14 to 39.
- Total mitigated candidates increased from 2077 to 2218.
- Changed rows dropped from 654 to 614.

Newly observed mitigations include bounded guards added while reviewing later
chunks, including pathology/classification terms, report/prose contexts,
short clinical abbreviations, and product/device contexts.

## PHI-Miss And Formatting Review

No residual email, dashed-phone, dotted-phone, or adjacent `<PHI> <PHI>`
pattern was found in `deidentified_output.csv`.

Four output-pattern review candidates were found:

- Four malformed URL-like strings, all `https://.`, in public TCGA source text.

The `https://.` strings were already present in the source text and are treated
as source artifacts, not confirmed PHI misses.

The previously observed UUID-like source artifact now remains intact:
`5849C91E-F4BB-4AA7-BE64-837168189379`. Its numeric tail is preserved by the
bounded `uuid_like_source_identifier` veto instead of being replaced with a
phone-shaped pyDeid surrogate.

No confirmed residual PHI miss was identified in this rerun.

## Current Review Candidates

There are 899 non-mitigated review candidates in the conservative heuristic
scan:

- 2 medium pipeline warnings from overlap-pruned spans in rows that still wrote
  successfully.
- 4 output-pattern artifacts, all malformed public-source `https://.` strings.
- 893 pyDeid fallback spans where replacement may remove source meaning.

The 893 pyDeid fallback spans break down heuristically as:

- 280 clinical/pathology precision candidates.
- 230 deferred location, institution, care-site, manufacturer-location, or
  source metadata candidates.
- 181 other precision candidates.
- 173 short-fragment candidates.
- 29 vendor/device precision candidates.

These buckets intentionally over-call review candidates. They are designed to
surface places where pyDeid changed text, not to assert that every replacement
is a bug.

## Issue Groups

### 1. Pipeline Warnings

Two sanitized warnings were emitted:

- one in `tcga-557`
- one in `pmc-7275215-1`

Both rows wrote output. The warning in `pmc-7275215-1` occurs in a geography-
and person-like COVID travel narrative with many pyDeid replacements. These
are medium-priority telemetry issues, not row failures.

Suggested handling: keep tracking in smoke reports. Do not change behavior
unless warnings increase or a warning row shows malformed output.

### 2. Public-Source URL Artifacts

Four `https://.` strings remain exactly as malformed public-source text.

Suggested handling: ignore the `https://.` strings. They are malformed
public-source artifacts, not residual contact information.

### 3. Deferred Location, Institution, Care-Site, And Source Metadata

Examples include `China`, `de`, `York`, `HRH`, `France`, `Palo`, `Beijing`,
`Somerville`, `Marlborough`, `India`, `Hamilton`, `Aarhus`, `Latham`,
`Province`, and source/citation author names such as `Carlson`, `Anderson`,
and `Burstein`.

Suggested handling: do not add broad built-in preservation. These strings can
be true site, geography, manufacturer-location, source/citation, or institution
identifiers in real notes. Preserve only through governed local policy or very
specific phrase/context rules.

### 4. Clinical Or Pathology Precision Candidates

Examples include `stich`, `long`, `Coller`, `sueperior`, `Entire`,
`EXAMINATION`, `Grocott`, `Carcinomas`, `stith`, `addendum`, `Frozen`,
`specimens`, `Furman`, `Storz`, `Ishak`, `Fite`, and source/citation author
names in breast HER2 reference boilerplate.

Many of these are OCR/source-template damage or author/reference metadata.
Some are clinically useful terms where additional bounded phrase variants may
be reasonable if they recur.

Suggested handling: no broad preserve. Potential future narrow candidates are
`Frozen` in frozen-section context, `Fite` in stain context, `Grocott` variants
not already covered, and `Furman` as a likely Fuhrman variant if recurrent and
clearly grading-related. Per current preference, skip OCR typo guards unless a
specific typo recurs enough to matter.

### 5. Short Fragments And OCR/Template Tokens

Examples include `at`, `to`, `PASH`, `and`, `La`, `by`, `au`, `mUI`, `is`,
`ell`, `SIH`, `DCY`, `URETER`, `SCN`, `PAP`, and similar fragments.

Some are valid abbreviations, some are source-template fragments, and some are
ordinary words or OCR damage.

Suggested handling: fix only recurring high-value clinical abbreviations with
strong context. Do not globally preserve short tokens. `PASH` may be worth a
future context-bound guard in breast pathology if it recurs.

### 6. Vendor, Device, And Manufacturer Metadata

Examples include `Judkins`, `Hilden`, `Armada`, `Kapa`, `Batson`, `Aarhus`,
`Minn`, `Redmond`, `Westbrook`, `Glidden`, `Kessler`, `Colgate`, `Beckman`,
`Haag`, `Streit`, `Smith`, and `Hain`.

Suggested handling: preserve only when product-specific context is strong.
Reasonable future candidates include `Judkins` in angiography catheter context,
`Glidden` in Gates-Glidden drill context, `Haag-Streit` in ophthalmic device
context, `Kapa` in sequencing/library-prep kit context, and `Armada` in
balloon/device context. Manufacturer locations such as `Aarhus`, `Redmond`,
`Westbrook`, and `Minn` should stay deferred.

### 7. Other Precision Candidates

Examples include `LASIK`, `Lasik`, `Whitacre`, `Touhy`/`Tuohy`, `Rinn`,
`Brescia`, `Salvati`, `Frisby`, `Palermo`, `mortis`, `apex`-like anatomy
fragments, and OCR/source-template fragments such as `overy`, `broun`,
`protain`, and `tobe`.

Suggested handling: this remains a mixed bucket. Some terms may be future
phrase-bound candidates if they recur and are clinically useful. Others are
names, public-source metadata, geography, or OCR/source-template damage and
should remain pyDeid fallback.

## Mitigations Observed

Project semantic-preservation policies accounted for 2218 mitigated candidate
rows:

- 917 stable date shifts
- 693 preserved date/time/year/score values
- 342 protected clinical terms
- 132 clinical-code/vendor/contextual vetoes
- 61 ordinary-token vetoes
- 39 ordinary clinical prose vetoes
- 24 clinical-abbreviation vetoes
- 7 title-context action-word vetoes
- 2 decimal-code/long-float contact vetoes
- 1 obstetric-history shorthand veto

## Follow-Up Items

1. Do not add broad preserves for geography, institutions, care sites,
   manufacturer locations, source/citation names, or short ambiguous tokens.
2. Consider future narrow guards for recurrent high-value clinical terms such
   as `PASH`, `Frozen` section context, `Fite` stain, `Judkins` catheter,
   `Gates-Glidden`, `Haag-Streit`, `KAPA` library-prep kits, and `LASIK`.
3. Keep OCR typo fixes deprioritized unless a specific typo repeats in real
   breast/pathology notes and materially affects downstream extraction.
