# Manual Review: External 1000-Note Eval

Date regenerated: 2026-06-09

## Scope And Method

Reran the current ProjectPHI CLI against the local 1000-note input file after
the chunk 4/5 semantic-preservation expansion. Regenerated
`deidentified_output.csv`, `audit_output.csv`, `audit_summary.csv`,
`issue_candidates.csv`, and `row_review_summary.csv`.

The review compared input rows, output rows, audit metadata, replacement
policies, warning rows, residual contact-like output patterns, candidate
contexts, and representative input/output diffs for changed rows. This remains
a smoke review without gold labels.

## Pipeline Summary

```text
rows_read=1000
rows_written=1000
rows_failed=0
spans_written=907
warnings=0
```

Input/output metadata matched for all 1000 rows. No output rows were empty.
109 rows changed through de-identification or date shifting.

Audit span labels: `NAME` 480, `DATE` 372, `TIME` 27, `LOCATION` 15,
`HOSPITAL` 11, `CONTACT` 2.

Replacement sources: `project_stable_date_shift` 281, `pyDeid` 145,
`project_protected_clinical_term` 126, `project_ordinary_token_veto` 123,
`preserved` 118, `project_clinical_code_veto` 73,
`project_ordinary_clinical_prose_veto` 25,
`project_clinical_abbreviation_veto` 15, and
`project_obstetric_history_veto` 1.

Row review statuses: 646 unchanged with no candidate flags, 317
mitigated-only, 10 changed with no candidate flags, 27 review candidates
present, and 0 high-priority review.

## PHI-Miss And Formatting Review

No high-priority semantic, formatting, or PHI-miss candidates were generated by
the smoke-review heuristics.

No obvious residual email, URL, dotted phone, dashed phone, or adjacent
`<PHI> <PHI>` artifact was found in `deidentified_output.csv`.

No confirmed PHI miss was identified in this rerun. Changed-row spot checks
showed expected stable date shifts and pyDeid fallback replacements for names,
locations, institutions, and care-site/source metadata.

## Fixed And Improved Issues

The previously fixed high-priority issues remain fixed:

- Numeric month/day placeholders already labeled by pyDeid as dates shift as
  partial dates, for example `8-29` -> `October 1`, `2-9` -> `February 29`,
  `4-20` -> `April 21`, and `6-17` -> `June 26`.
- Compact clinical codes and staging examples such as `E2V2M5`, `T3N0M0`,
  karyotype `XY`/`del`, and genomic coordinate fragments are preserved when
  clinical context supports that read.
- Clinical terms, phrase-bound eponyms, device/vendor references, pathology
  abbreviations, and ordinary clinical prose added through the chunk 4/5
  preservation passes are now reflected in the audit output.
- No unparseable date-placeholder warnings were emitted.

Current mitigation counts:

- pyDeid fallback replacement rows: 145
- protected clinical-term mitigations: 126
- clinical-code/vendor-reference mitigations: 73
- ordinary-token vetoes: 123
- total mitigated candidates: 762

## Current Issues

There are 48 remaining medium-priority review candidates. These are not
confirmed PHI misses; they are cases where pyDeid replacement may remove useful
source or semantic meaning.

Main groups:

- Geography, institution, care-site, or source/manufacturer location metadata:
  44 candidates. Examples include `Aloag`, `Tambillo`, `Quito`, `York`,
  `Palo`, `Cuneo`, `Irvine`, `Madison`, `Padua`, `Rome`, `Paris`, `Madrid`,
  `Milan`, `ShenZhen`, `China`, `Guangzhou`, `Tustin`, `Marlborough`,
  `Mississauga`, `Lyon`, `France`, `Nanjing`, `Homburg`, `Oakwood`, and
  non-anatomic `de`/`La` in institution or manufacturer-location contexts.
- Four remaining short-fragment candidates:
  - `XY` twice in `Ms. XY` placeholder/person-like contexts.
  - `of` in `resident of Tambillo`, adjacent to location metadata.
  - `JJC` in `JJC staff`, a source/institution-like staff reference.

The remaining short fragments are deliberately not broadened. `Ms. XY` is
person-like rather than karyotype notation, `of` appears in a location phrase,
and `JJC staff` is institution/source-like.

## Representative Input/Output Checks

- `Ms. XY` became `Ms. Alexis`; this is acceptable because the context is a
  person-like placeholder, not karyotype notation.
- `resident of Tambillo` had `of` replaced; this is a minor formatting/semantic
  artifact adjacent to intentionally deferred geography.
- `JJC staff` became `Michael staff`; this is a source/institution-like staff
  reference and is not a breast/oncology semantic priority.
- Public geography and manufacturer/source locations remain pyDeid fallback,
  for example `Paris to Madrid`, `Palo Alto`, `Irvine`, `Milan`, `Nanjing`,
  `Homburg`, and `Oakwood`.

## Follow-Up Items

1. Keep geography, institution, care-site, and source/manufacturer location
   names as pyDeid fallback unless governed local policy explicitly decides
   otherwise.
2. Leave person-like placeholders such as `Ms. XY` to pyDeid fallback.
3. Do not globally preserve short fragments such as `of`; only add future
   phrase-bound rules if they recur in real target notes with low identifier
   risk.
4. Treat source/institution-like staff references such as `JJC staff` as
   governed local-policy items, not public built-in preserves.
